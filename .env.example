# Optional: set to enable LLM-based extraction (much higher quality)
# Free tier / minimal credits work fine — uses claude-haiku (cheapest model)
ANTHROPIC_API_KEY=

# n8n settings (only needed for Docker workflow)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=claraai123
